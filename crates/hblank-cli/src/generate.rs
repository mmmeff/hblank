use std::{
    fmt::Write as _,
    fs,
    path::{Path, PathBuf},
};

use thiserror::Error;

use crate::{Config, DiscoveredExample, DiscoveryError, discover_examples};

pub const GENERATED_EXAMPLES_PATH: &str = ".hblank/generated/examples.rs";

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct GenerationResult {
    pub examples: Vec<DiscoveredExample>,
    pub changed: bool,
}

/// Discovers examples and atomically refreshes the generated Rust imports when needed.
///
/// # Errors
/// Returns an error when discovery or generated-file IO fails.
pub fn refresh_generated_examples(
    project_root: &Path,
    config: &Config,
) -> Result<GenerationResult, GenerationError> {
    let examples = discover_examples(project_root, config)?;
    let source = generated_source(&examples);
    let path = project_root.join(GENERATED_EXAMPLES_PATH);
    let changed = fs::read(&path).map_or(true, |current| current != source.as_bytes());
    if changed {
        write_atomically(&path, source.as_bytes())?;
    }
    Ok(GenerationResult { examples, changed })
}

#[must_use]
pub fn generated_source(examples: &[DiscoveredExample]) -> String {
    let mut source = String::from(
        "// Generated by hblank dev. Do not edit.
",
    );
    for example in examples {
        let path = rust_string(&example.absolute_path);
        source.push_str(
            "#[allow(dead_code, unused_imports)]
",
        );
        write!(
            &mut source,
            "#[path = {path}]\nmod {};\n",
            example.module_name
        )
        .expect("writing generated imports to a String cannot fail");
    }
    source
}

fn rust_string(path: &Path) -> String {
    format!("{:?}", path.to_string_lossy())
}

fn write_atomically(path: &Path, contents: &[u8]) -> Result<(), GenerationError> {
    let parent = path
        .parent()
        .ok_or_else(|| GenerationError::MissingParent(path.to_path_buf()))?;
    fs::create_dir_all(parent).map_err(|source| GenerationError::CreateDirectory {
        path: parent.to_path_buf(),
        source,
    })?;
    let temporary = parent.join(format!(".examples.rs.{}.tmp", std::process::id()));
    fs::write(&temporary, contents).map_err(|source| GenerationError::Write {
        path: temporary.clone(),
        source,
    })?;
    if let Err(source) = fs::rename(&temporary, path) {
        let _ = fs::remove_file(&temporary);
        return Err(GenerationError::Write {
            path: path.to_path_buf(),
            source,
        });
    }
    Ok(())
}

#[derive(Debug, Error)]
pub enum GenerationError {
    #[error(transparent)]
    Discovery(#[from] DiscoveryError),
    #[error("generated example path has no parent: {0}")]
    MissingParent(PathBuf),
    #[error("could not create generated source directory {path}: {source}")]
    CreateDirectory {
        path: PathBuf,
        source: std::io::Error,
    },
    #[error("could not write generated example source {path}: {source}")]
    Write {
        path: PathBuf,
        source: std::io::Error,
    },
}
